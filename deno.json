{
  "imports": {
    "@/": "./",
    "hash-wasm": "npm:hash-wasm/dist/index.esm.min.js"
  },
  "tasks": {
    "build:init": "mkdir -p ./dist && mkdir -p ./deps",
    "build:zig": "cd ./deps && zig build-exe -O ReleaseSafe ../src/main.zig --name age",
    "build:ghash": "cd ./deps && zig build-exe -O ReleaseSmall -target wasm32-freestanding ../src/stream/ghash.zig -flto -fno-builtin -fno-entry --export=Ghash_init --export=Ghash_update --export=Ghash_pad --export=Ghash_final",
    "build:aes": "cd ./deps && zig build-exe -O ReleaseSmall -target wasm32-freestanding ../src/stream/aes-gcm.zig -flto -fno-builtin -fno-entry --export=Aes_init --export=Aes_update --export=Aes_final",
    "test:ghash": "deno test -R=. ./src/stream/ghash.js",
    "test:aes": "deno test -R=. ./src/stream/aes-gcm.test.js",
    "build:gui": "cargo build --release && mv target/release/age.exe ./dist/age-gui.exe",
    "build": "deno task build:init && deno task build:zig && cp ./deps/age.exe ./dist/age.exe && deno task build:gui",
    "ui": "cd ./dist && age-gui",
    "e": "./dist/age e",
    "d": "./dist/age d"
  }
}